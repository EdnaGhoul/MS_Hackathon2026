# Daily run — scheduled prompt template
# Replace every {{placeholder}} from config before registering with the scheduler. Read it back; none may remain.

Build {{identity.name}}'s Exec Command Center for today and deliver it as ONE JSON file in their OneDrive folder "{{storage.folder}}" (OneDrive root). Write it twice: brief-YYYY-MM-DD.json (new, history) and latest.json (download, replace contents, upload in place). The Exec Command Center app renders latest.json, so follow EXACTLY the schema in the skill's references/brief-schema.json (schemaVersion {{schemaVersion}}); read the existing latest.json first and keep every key. Do not alter the app.

Executive: {{identity.name}}, {{identity.role}}; manager {{identity.manager}}; time zone {{identity.timeZone}}. Lens: {{lens}} (use its action verbs). Team mode: {{team.mode}}; quiet after {{team.quietAfterDays}} days; 1:1 keywords {{team.oneToOneKeywords}}. Named accounts: {{accounts}}. Personal/protected calendar: organisers {{protectedCalendar.personalOrganisers}}, keywords {{protectedCalendar.personalKeywords}}, own blocks {{protectedCalendar.ownBlocks}}. KPI thresholds (fixed, publish in each tile): {{thresholds}}. Write-back level: {{writeBack}} (drafts only — never send, post, accept, decline or move).

Follow the skill's references/section-playbooks.md for every section, references/meeting-brief.md for a brief on every event, and references/design-principles.md for the twenty rules. Mandatory: calendar completeness (today bounded, next 5 working days one per request, every continuation followed, every event included, personal masked, count reconciled and stated in meta.evidenceNote); sent items of the last 24 h checked before writing decisions; every inbox item, decision link, pre-read and source linked with the "Open in Outlook"/Teams link returned by m365_search-SearchM365 (the mailbox listing returns no links — resolve each item through search; never construct a URL; leave empty and say so only if search returns none); a reply draft (never sent) created or reused for every unanswered email that needs a reply, linked in draftUrl; state-*.json of the last 7 days read first and carried forward; unanswered requests/brief-*.json answered into briefs/. Validate with scripts/validate_brief.py before writing.

On {{cadence.reviewDay}} fill weekInReview from the brief-*.json history (what moved, commitments closed vs opened, accepted-hours trend, team gone quiet, three things to set up next week, six-week series for the four KPIs).

If the folder cannot be written, fall back to a self-contained HTML dashboard and say so. Reply with a one-screen summary under 300 words that opens with the governing answer and lists the decisions required. Only state facts traceable to mail, chats, calendar or transcripts; say when a source returns nothing. Never send messages or change calendar items.
